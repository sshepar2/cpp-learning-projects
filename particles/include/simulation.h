#ifndef SIMULATION_H
#define SIMULATION_H

// alternatively can use #pragma once, and remove the #ifndef, #define, #endif tags.

#include <vector>
#include "particle.h"

void printSimulationSummary(std::vector<Particle*> particlePointers);

#endif
